samples('github:tidalcycles/dirt-samples')
samples('github:switchangel/pad')

stack(
  s("breaks125").fit().slice([0,.25,.5,.75], "0 1 1 <2 3>").gain(.2),
  s("swpad:0").scrub("{0.1!2 .25@3 0.7!2 <0.8:1.5>}%8").slow(8).gain(.1)
)